# E4 Industrial External Validation

The final public unified model will be evaluated on each real enterprise
battery family with:

    zero-shot
    pretrained + 1-cell / 2-cell / ... adaptation
    scratch + exactly the same target cells

The zero_shot and few_cell subdirectories are protocol homes only. No
enterprise source is fabricated to make them runnable.
