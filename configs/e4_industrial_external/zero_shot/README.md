# E4 zero-shot interface

Add one config per real enterprise battery domain after its raw adapter, split
and provenance are validated. The config must declare the final-public source
domains and one target enterprise domain. It must not use enterprise target
test data to select a checkpoint.
