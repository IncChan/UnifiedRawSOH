# Paper-v2 configurations

This namespace is intentionally empty until the Paper-v1 representation,
residual, and gradient-conflict diagnostics are complete. V2 configs will not
override V1 configs and will use `output.paper_version = "Paper-v2"` so their
runtime products remain under `outputs/Paper-v2/`.
