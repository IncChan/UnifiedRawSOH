# E3 cross-domain scripts

There is intentionally no generic E3 training command yet. A transfer command
must load a source-only checkpoint, draw the target development budget
deterministically, preserve target test isolation, and run the same-target-
budget scratch comparator. The current config validator records that contract
without pretending the E1 trainer provides it.
