# Conditional health-aware contrastive extension

Do not use this directory as an independent E5/E6/E7 experiment. It becomes
relevant only if E2 shows that Unified is materially worse than Separate or
z_health clusters mainly by domain. The current model already exposes encode()
and z_health; no contrastive objective or launcher has been implemented.
